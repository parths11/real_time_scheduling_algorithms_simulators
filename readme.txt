rts_project1 contains codes for the simulators of rate monotonic, earliest deadline first and rate monotonic with deferrable server scheduling algorithm.

rts_project2 contains codes for the simulators of priority ceiling protocol, immediate ceiling priority protocol and highest value density first scheduling algorithms.